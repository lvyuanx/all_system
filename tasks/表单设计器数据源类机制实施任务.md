# 表单设计器数据源类机制实施任务

## Context

- 原始需求文档：`tasks/表单设计器数据源类机制需求文档.md`
- 目标范围：
  - `flow_engine/templates/flow_engine/form_designer.html`
  - `flow_engine/templates/flow_engine/flow_designer.html`
  - `flow_engine/utils/form_runtime_util.py`
  - `flow_engine/views/`
  - `flow_engine` 配置与新增 `data_sources` 模块
- 注意事项：
  - 保持历史 `default_config/options_config` 兼容
  - 新机制优先于旧机制
  - 数据源类只读，不允许写回上下文
  - 错误提示使用中文

## Tasks

- [x] 定义字段数据源协议与注册中心
  - 新增 `flow_engine/data_sources/base.py`
  - 新增 `flow_engine/data_sources/registry.py`
  - 增加 `FLOW_ENGINE_FIELD_DATA_SOURCES` 配置读取
  - 校验 `key` 唯一、`data_type` 合法、`support_components` 合法

- [x] 提供至少 3 个内置数据源示例
  - 上下文文本默认值数据源
  - 订单字段文本默认值数据源
  - 站点地址下拉选项数据源

- [x] 改造运行时表单解析逻辑接入数据源机制
  - 在 `form_runtime_util.py` 中优先解析 `default_source_config/options_source_config`
  - 为数据源实例注入 `self.ctx/request/field_schema/instance/node_code`
  - 默认值和选项列表都支持新数据源类机制
  - 保留旧 `default_config/options_config` 回退逻辑

- [x] 新增字段数据源元信息接口
  - 提供已注册数据源的元信息列表
  - 返回 `key/label/data_type/support_components/support_default/support_options/params_schema`
  - 接口输出可直接供设计器加载使用

- [x] 改造表单设计器与流程设计器字段配置 UI
  - 默认值获取方式改成“固定值 / 数据源类”
  - 选项获取方式改成“手工选项 / 数据源类”
  - 根据字段组件过滤可选数据源类
  - 根据 `params_schema` 动态渲染参数表单
  - 保留旧配置兼容展示与保存

- [x] 补充测试与基础文档
  - 增加注册中心测试
  - 增加运行时解析测试
  - 增加上下文注入测试
  - 增加新增数据源类的开发说明

