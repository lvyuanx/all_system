# 表单设计器数据源类机制补齐任务

## 背景

- 以 `tasks/表单设计器数据源类机制需求文档.md` 为唯一需求基准。
- 当前仓库里已经有一部分后端尝试实现，但与需求仍有偏差。
- 你的任务不是沿着旧 `source_type/context_path/enum_code/db_source_code` 继续扩展，而是把实现拉回到“固定值 + 数据源类”的目标方案。

## 必须修正的偏差

- [x] 把数据源基类和注册中心拆到 `flow_engine/data_sources/base.py` 与 `flow_engine/data_sources/registry.py`
- [x] 注册中心必须支持：
  - 加载 `FLOW_ENGINE_FIELD_DATA_SOURCES`
  - 校验是否继承基类
  - 校验 `key` 唯一
  - 校验 `data_type` 合法
  - 校验 `support_components` 合法
- [x] 数据源类协议必须包含：
  - `key`
  - `label`
  - `data_type`
  - `support_components`
  - `support_default`
  - `support_options`
  - `params_schema`
  - `get_default(request)`
  - `get_options(request)`
- [x] 运行时实例必须能访问：
  - `self.ctx`
  - `self.request`
  - `self.field_schema`
  - `self.instance`
  - `self.node_code`
- [x] 新配置优先级必须是：
  - `default_source_config/options_source_config`
  - 其次 `default_config/options_config`
  - 再次 `default/options`

## 前端必须达成的结果

- [x] 表单设计器和流程设计器里，默认值获取方式只展示：
  - 固定值
  - 数据源类
- [x] 选项来源只展示：
  - 固定选项
  - 数据源类
- [x] 当选择数据源类时：
  - 展示数据源类下拉框
  - 根据 `params_schema` 动态渲染参数表单
  - 默认值支持 `fallback_value`
- [x] 不要继续把 `context_path/enum_code/db_source_code` 作为主配置交互暴露给用户
- [x] 旧配置只作为兼容层读取，不作为主交互模型

## 接口与元信息

- [x] 提供字段数据源元信息接口
- [x] 返回字段必须包含：
  - `key`
  - `label`
  - `data_type`
  - `support_components`
  - `support_default`
  - `support_options`
  - `params_schema`

## 错误与兼容

- [x] 用户可见提示尽量使用中文
- [x] 数据源执行失败时页面不能崩溃
- [x] 默认值型数据源失败时优先回退 `fallback_value`
- [x] 选项型数据源失败时返回空列表，或按配置回退手工选项
- [x] 历史 `default_config/options_config/context_binding` 必须兼容

## 示例与测试

- [x] 提供至少 3 个内置数据源类示例
- [x] 增加注册中心测试
- [x] 增加运行时注入测试
- [x] 增加兼容性测试
- [x] 增加设计器元信息接入测试

## 执行约束

- 只修改与该需求直接相关的 `flow_engine` 文件和必要测试。
- 不要继续扩展旧版 `legacy_config` 示例作为主要方案。
- 若发现上一次自动改动与本需求冲突，以本任务说明和需求文档为准做调整。
